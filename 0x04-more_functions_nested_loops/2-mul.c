/**
*mul - return the product of two integers
*@a: integer one (to multiply two)
*@b: integer two (to by multiplied by one)
*Return: value of the product 
*/
int mul(int a, int b)
{
	return (a * b);
}
